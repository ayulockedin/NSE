# ── NSE core (lightweight, required to run the Day-1 foundation) ──
pydantic>=2.6
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
networkx>=3.2
tree-sitter>=0.21
tree-sitter-python>=0.21
docker>=7.0
numpy>=1.26
scikit-learn>=1.4
diff-match-patch>=20230430

# ── ML stack (latent transition model) ──
torch>=2.2
torch-geometric>=2.5

# ── dev / test ──
pytest>=8.0
ruff>=0.4
mypy>=1.9
coverage>=7.0          # coverage-as-uncertainty signal (Phase 7.1); optional — degrades to no-op if absent

# ── inference host (NOTE: vLLM has no native Windows wheels) ──
# Install on Linux/WSL only:
# vllm>=0.4
